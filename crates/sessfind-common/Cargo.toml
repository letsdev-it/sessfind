[package]
name = "sessfind-common"
version = "0.2.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
dirs = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
